{
  "name": "employee-management-backend",
  "version": "1.0.0",
  "description": "High-performance Employee Management API with Fastify",
  "main": "dist/server.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "tsx watch --clear-screen=false src/server.ts 2>nul",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts --quiet",
    "format": "prettier --write src",
    "clean": "rmdir /s /q dist 2>nul || echo Clean completed",
    "db:generate": "prisma generate --no-hints",
    "db:push": "prisma db push --accept-data-loss --skip-generate > nul 2>&1",
    "db:seed": "tsx src/seed.ts",
    "db:reset": "prisma db push --force-reset && yarn db:seed"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@prisma/client": "^5.13.0",
    "concurrently": "^9.0.2",
    "fastify": "^5.2.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/supertest": "^6.0.2",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.0",
    "prisma": "^5.13.0",
    "supertest": "^6.3.3",
    "tsx": "^4.7.0",
    "typescript": "~5.3.3",
    "vitest": "^1.2.0"
  }
}
